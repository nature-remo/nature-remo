{
  "name": "nature-remo",
  "version": "0.2.0",
  "description": "Nature Remo Cloud API wrapper.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && mocha",
    "docs": "typedoc --mode modules --out docs --excludeNotExported --excludeExternals --includeDeclarations src && touch ./docs/.nojekyll"
  },
  "engines": {
    "node": ">=6.14.3"
  },
  "keywords": [
    "nature-remo"
  ],
  "files": [
    "dist/"
  ],
  "author": "Yasuaki Uechi <y@uechi.io> (https://uechi.io/)",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "@types/node": "^10.5.2",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "typedoc": "^0.11.1",
    "typescript": "^2.9.2"
  }
}
