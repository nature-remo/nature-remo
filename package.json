{
  "name": "nature-remo",
  "description": "Nature Remo API client",
  "version": "0.3.1",
  "author": "Yasuaki Uechi <y@uechi.io> (https://uechi.io/)",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "rm -r ./lib && tsc",
    "docs": "yarn build && typedoc --mode file --out docs --excludeExternals  --excludePrivate --exclude '**/__tests__/*' src && touch ./docs/.nojekyll",
    "lint": "tslint --project ./tsconfig.json --fix && prettier 'src/**/*.ts' --write",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.9",
    "@types/nock": "^9.3.1",
    "@types/node": "^11.10.4",
    "jest": "24.1.0",
    "nock": "^10.0.6",
    "ts-jest": "^24.0.0",
    "tslint": "^5.13.1",
    "typedoc": "^0.14.2",
    "typescript": "^3.3.3333"
  },
  "engines": {
    "node": ">=8.14.3"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "nature-remo",
    "smart-home"
  ],
  "license": "MIT"
}
