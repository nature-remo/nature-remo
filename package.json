{
  "name": "nature-remo",
  "version": "0.3.0",
  "description": "Nature Remo Cloud API wrapper.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -r ./dist && tsc",
    "test": "npm run build && mocha",
    "docs": "npm run build && typedoc --mode file --out docs --excludeExternals --excludeNotExported --excludePrivate --includeDeclarations src && touch ./docs/.nojekyll"
  },
  "engines": {
    "node": ">=6.14.3"
  },
  "keywords": [
    "nature-remo"
  ],
  "files": [
    "dist/"
  ],
  "author": "Yasuaki Uechi <y@uechi.io> (https://uechi.io/)",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "@types/node": "^10.5.2",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "typedoc": "^0.12.0",
    "typescript": "^3.0.1"
  }
}
